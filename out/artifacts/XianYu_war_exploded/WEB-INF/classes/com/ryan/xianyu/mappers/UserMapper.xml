<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="user">

    <select id="selectByUserName" resultType="User" parameterType="java.lang.String">
        SELECT * FROM user WHERE username = #{username};
    </select>

    <select id="selectById" resultType="User" parameterType="java.lang.Integer">
        SELECT * FROM user WHERE id = #{id};
    </select>

    <select id="selectByIds" resultType="User" parameterType="java.lang.String">
        SELECT * FROM user WHERE id in (#{id});
    </select>


    <insert id="insertNewUser" useGeneratedKeys="true" parameterType="User">
        INSERT INTO
        user(username, password, name, phone, institute_id, stu_id, email, admin)
        VALUE (#{user.username},#{user.password},#{user.name},#{user.phone},
        #{user.instituteId},#{user.stuId},#{user.email}, 0);
    </insert>
</mapper>